超シンプルなブラウザベース動作アプリを作りたい

・ローカルwebサーバとして起動して、ブラウザを起動して自分に接続させる

・C++ベースでミニマムな実装
　APIを拡張できるプラグイン構造にしておく

・UIは全てHTML+Javascriptベースで実装する。
　開発中はカレントにある web フォルダを参照して動作
  リリース時はそれをまるごと zip化して実行ファイルとくっつけて提供

・localhost:適当なポートでサーバを起動してブラウザをそこにつながせる（ポートやホストはコマンドラインで変更可）
・ブラウザ起動は chrome / edge を -app モードでの起動をまず試みて、ダメだった場合は標準ブラウザにつながせる
・ブラウザからの接続が無くなって一定時間で自動終了（プロセスが残るのを防止）

現在吉里吉里に WebServer として組み入れてる処理を元にしてつくれるか？
d:/work/kirikiri/krkrz 以下の吉里吉里全体のソースあり

開発テスト用にサーバ起動側から状態確認できる REPL 機構も準備して
AIコーディングしやすくする。ブラウザからはサーバAPIをポーリングする
機構を標準でくみいれることで、REPLからブラウザ側のコマンド制御などもできるようにする

ベースアプリは、とりあえずローカルファイルアクセス機能を準備
それに対応した UI側でのファイル/ディレクトリ選択とファイルからのデータ参照を実装

これベースで作る他のアプリは、このアプリを基本構造として cmake でとりこんで、
独自の server api と独自UIで動作するという想定。

現在思案中のアプリ例:
psdparse ライブラリ（d:/test/psdparse ）をAPI状態で提供した上で、
Photoshopの中身確認、レイヤ名変更、テキストレイヤの内容変更ができるアプリ。
ツリー上で変更の他、csvエクスポート/インポートで一括処理もできるようにする

他、各種ツール用アプリを今 python や C# でつくってみたりしているのだが、
AIコーディングで C++ライブラリのAPI化が容易になるなら、
アプリのUI制御とメイン処理はブラウザ上のスクリプトだけで全部できるのではという想定


